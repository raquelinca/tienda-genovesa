export const XmlDateTimeConverter = {
    set: (value) => new Date(value),
    get: (value) => {
        if (value) {
            return value.toISOString();
        }
        return undefined;
    },
};
