"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.XmlCollection = void 0;
const collection_js_1 = require("./collection.js");
const error_js_1 = require("./error.js");
const utils_js_1 = require("./utils.js");
const xml_object_js_1 = require("./xml_object.js");
class XmlCollection extends xml_object_js_1.XmlObject {
    constructor() {
        super(...arguments);
        this.MaxOccurs = Number.MAX_VALUE;
        this.MinOccurs = 0;
        this.items = [];
    }
    HasChanged() {
        const res = super.HasChanged();
        const changed = this.Some((item) => item.HasChanged());
        return res || changed;
    }
    get Count() {
        return this.items.length;
    }
    Item(index) {
        return this.items[index] || null;
    }
    Add(item) {
        this.items.push(item);
        this.element = null;
    }
    Pop() {
        this.element = null;
        return this.items.pop();
    }
    RemoveAt(index) {
        this.items = this.items.filter((item, index2) => index2 !== index);
        this.element = null;
    }
    Clear() {
        this.items = [];
        this.element = null;
    }
    GetIterator() {
        return this.items;
    }
    ForEach(cb) {
        this.GetIterator().forEach(cb);
    }
    Map(cb) {
        return new collection_js_1.Collection(this.GetIterator().map(cb));
    }
    Filter(cb) {
        return new collection_js_1.Collection(this.GetIterator().filter(cb));
    }
    Sort(cb) {
        return new collection_js_1.Collection(this.GetIterator().sort(cb));
    }
    Every(cb) {
        return this.GetIterator().every(cb);
    }
    Some(cb) {
        return this.GetIterator().some(cb);
    }
    IsEmpty() {
        return this.Count === 0;
    }
    OnGetXml(element) {
        for (const item of this.GetIterator()) {
            const el = item.GetXml();
            if (el) {
                element.appendChild(el);
            }
        }
    }
    OnLoadXml(element) {
        const self = this.GetStatic();
        if (!self.parser) {
            throw new error_js_1.XmlError(error_js_1.XE.XML_EXCEPTION, `${self.localName} doesn't have required 'parser' in @XmlElement`);
        }
        for (let i = 0; i < element.childNodes.length; i++) {
            const node = element.childNodes.item(i);
            if (!((0, utils_js_1.isElement)(node) &&
                node.localName === self.parser.localName &&
                node.namespaceURI == self.namespaceURI)) {
                continue;
            }
            const el = node;
            const item = new self.parser();
            item.LoadXml(el);
            this.Add(item);
        }
    }
}
exports.XmlCollection = XmlCollection;
